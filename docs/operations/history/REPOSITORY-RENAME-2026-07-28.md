# Website Repository Rename Record — 2026-07-28

GitHub readback on 2026-07-28 confirmed that the canonical Website repository
is `Mochirii-Wushu/Mochirii-Website`. The former slug
`Mochirii-Wushu/Mochirii` redirects for ordinary repository traffic, but it is
not the canonical source identity and must not be emitted by active workflows,
runtime metadata, security contacts, documentation links, or release tooling.

All active repository references and local remotes must use the canonical
name. Existing dated evidence under `reports`, `docs/operations/evidence`, and
`docs/operations/history` may retain the exact former slug when it records a
historical command, approval, URL, or provider readback. Those records must not
be silently rewritten because their dates, commits, and approval wording are
part of the evidence. This record supplies the current-name interpretation for
those retained references.

GitHub recommends updating local remotes after a rename and notes that GitHub
Actions references are not redirected. See [Renaming a
repository](https://docs.github.com/en/repositories/creating-and-managing-repositories/renaming-a-repository).
